- name: install packages
  hosts: local
  connection: local
  tasks:
    - name: install packages
      ansible.builtin.package:
        name: " install {{ item }}"
      loop:
        - gcc
        - zip
        - mysql
        - nginx