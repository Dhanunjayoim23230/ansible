- name: install packages
  hosts: local
  connection: local
  tasks:
    - name: install packages
      ansible.builtin.packages:
        name: " install {{ item }}"
      loop:
        - gcc
        - zip
        - mysql
        - nginx